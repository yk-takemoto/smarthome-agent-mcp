{
    "name": "controlLightBySwitchbot",
    "description": "Control light given command by Switchbot.",
    "inputSchema": {
        "type": "object",
        "properties": {
            "commandTarget": {
                "type": "string",
                "description": "The Command target to control light. e.g. if target is 'living room' then commandTarget='main', else if target is 'next to the living room' then commandTarget='next', else commandTarget='other'"
            },
            "commandTurning": {
                "type": "string",
                "description": "The Command to send power On/Off. e.g. 'turnOn', 'turnOff'"
            }
        },
        "required": [
            "commandTarget"
        ]
    }
}